{
  "name": "server",
  "version": "1.0.0",
  "description": "This is the backend component of the **Screen Time Recorder**, a smart study tracking tool that differentiates effective study time from passive screen time. It receives data from the frontend (screen usage, eye tracking, focus levels), processes it, and stores study activity patterns.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
